<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <insert id="insert">
        INSERT INTO clazz (
        name,
        room,
        begin_date,
        end_date,
        master_id,
        subject,
        create_time,
        update_time
        ) VALUES (
        #{name},
        #{room},
        #{beginDate},
        #{endDate},
        #{masterId},
        #{subject},
        #{createTime},
        #{updateTime}
        )
    </insert>
    <update id="update">
        UPDATE clazz
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="room != null">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM clazz WHERE id = #{id}
    </delete>

    <select id="findAll" resultType="com.itheima.pojo.Clazz">

        SELECT
        c.id,
        c.name,
        c.room,
        c.begin_date AS beginDate,
        c.end_date AS endDate,
        c.master_id AS masterId,
        c.subject,
        c.create_time AS createTime,
        c.update_time AS updateTime,
        e.name AS masterName,
        CASE
        WHEN CURDATE() &gt; c.begin_date then '未开班'
        WHEN CURDATE() BETWEEN c.begin_date AND c.end_date THEN '在读'
        ELSE '已结课'
        END AS status
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        ORDER BY c.update_time DESC
    </select>
    <select id="selectClazzPage" resultType="com.itheima.pojo.Clazz">
        select
        c.id,
        c.name,
        c.room,
        c.begin_date,
        c.end_date,
        c.master_id,
        c.subject,
        c.create_time,
        c.update_time,
        e.name masterName,
        (case
        when begin_date &gt;now() then '未开班'
        when now() &gt; end_date then '已结课'
        else  '在读中' end ) status
        from clazz c left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and end != null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>
    <select id="selectById" resultType="com.itheima.pojo.Clazz">
        SELECT
        c.id,
        c.name,
        c.room,
        c.begin_date AS beginDate,
        c.end_date AS endDate,
        c.master_id AS masterId,
        c.subject,
        c.create_time AS createTime,
        c.update_time AS updateTime,
        e.name AS masterName,
        CASE
        WHEN CURDATE() &gt; c.begin_date THEN '未开班'
        WHEN CURDATE() BETWEEN c.begin_date AND c.end_date THEN '在读'
        ELSE '已结课'
        END AS status
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        WHERE c.id = #{id}
    </select>
    <select id="selectByName" resultType="com.itheima.pojo.Clazz">
        SELECT
        id,
        name,
        room,
        begin_date AS beginDate,
        end_date AS endDate,
        master_id AS masterId,
        subject,
        create_time AS createTime,
        update_time AS updateTime
        FROM clazz
        WHERE name = #{name}
    </select>

</mapper>